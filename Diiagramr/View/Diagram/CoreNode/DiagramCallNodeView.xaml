<UserControl x:Class="Diiagramr.View.Diagram.CoreNode.DiagramCallNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:coreNode="clr-namespace:Diiagramr.ViewModel.Diagram.CoreNode"
             d:DataContext="{d:DesignInstance coreNode:DiagramCallNodeViewModel}"
             d:DesignWidth="40"
             d:DesignHeight="40"
             Background="White"
             PreviewMouseDoubleClick="{s:Action HandlePreviewMouseDoubleClick}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="Btv" />
    </UserControl.Resources>
    <Label Visibility="{Binding BrokenDueToRecursion, Converter={StaticResource Btv}}" Content="Recursive"
           FontSize="12" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Foreground="DarkRed" />
</UserControl>