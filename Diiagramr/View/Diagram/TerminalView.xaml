<UserControl x:Class="Diiagramr.View.Diagram.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:diagram1="clr-namespace:Diiagramr.ViewModel.Diagram"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance diagram1:TerminalViewModel}"
             Width="14" 
             Height="14">
    <Border AllowDrop="True"
            Drop="{s:Action DropEventHandler}"
            MouseMove="{s:Action MouseMove}"
            MouseDown="{s:Action TerminalMouseDown}"
            CornerRadius="2">
        <Border.Style>
            <Style>
                <Setter Property="Border.Background" Value="LightSteelBlue"/>
                <Style.Triggers>
                    <Trigger Property="Border.IsMouseOver" Value="True">
                        <Setter Property="Border.Background" Value="GhostWhite" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
    </Border>
</UserControl>
